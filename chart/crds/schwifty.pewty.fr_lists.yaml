---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: lists.schwifty.pewty.fr
spec:
  group: schwifty.pewty.fr
  names:
    kind: List
    listKind: ListList
    plural: lists
    singular: list
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: List is the Schema for the lists API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            items:
              description: ListSpec defines the desired state of List
              properties:
                items:
                  items:
                    properties:
                      key:
                        type: string
                      label:
                        type: string
                      selectable:
                        type: boolean
                      type:
                        type: string
                      width:
                        type: integer
                    required:
                    - key
                    - label
                    - selectable
                    - type
                    - width
                    type: object
                  type: array
                resourceSelector:
                  type: string
              required:
              - items
              - resourceSelector
              type: object
            type: array
          status:
            description: ListStatus defines the observed state of List
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
